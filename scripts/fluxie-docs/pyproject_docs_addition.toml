# -----------------------------------------------------------------------
# ADD THIS BLOCK to the [project.optional-dependencies] section of your
# existing pyproject.toml.
#
# Example of what the section looks like after the addition:
#
#   [project.optional-dependencies]
#   docs = [
#       "sphinx>=7.0",
#       ...
#   ]
#
# Install locally with:
#   pip install -e ".[docs]"
# -----------------------------------------------------------------------

[project.optional-dependencies]
docs = [
    "sphinx>=7.0",
    "sphinx-rtd-theme>=2.0",
    "sphinx-autodoc-typehints>=1.25",
    "myst-parser>=2.0",
    "nbsphinx>=0.9",
    "nbsphinx-link>=1.3",
]
